import path from 'path';

export default {
    default: {
        path: path.resolve('./src/components/'),
        files: {
            main: 'index.js',
            styles: 'styles.less'
        }
    }
};
